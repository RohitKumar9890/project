# Root Environment Example
# This file is for reference only - actual configuration happens in:
# - server/.env (backend configuration)
# - client/.env.local (frontend configuration)

# Quick Setup:
# 1. cp server/.env.example server/.env
# 2. cp client/.env.local.example client/.env.local
# 3. Edit the files above with your configuration
# 4. Run: npm run generate-secrets (for JWT secrets)
# 5. Setup Firebase and download service account JSON
# 6. Run: npm run seed:admin (create admin user)
# 7. Run: npm run dev (start development servers)

# For detailed setup instructions, see README.md
